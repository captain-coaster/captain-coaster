<div class="navbar navbar-inverse navbar-fixed-top bg-indigo">
    <div class="navbar-header">
        <a class="navbar-brand" href="{{ path('bdd_index') }}">
            <img src="{{ asset('images/logo_light.png') }}" alt="Coasters World"></a>
        <ul class="nav navbar-nav visible-xs-block">
            <li><a data-toggle="collapse" data-target="#navbar-mobile"><i class="icon-tree5"></i></a></li>
            <li><a class="sidebar-mobile-main-toggle"><i class="icon-paragraph-justify3"></i></a></li>
        </ul>
    </div>
    <div class="navbar-collapse collapse" id="navbar-mobile">
        {#<ul class="nav navbar-nav">#}
            {#<li>#}
                {#<a class="sidebar-control sidebar-main-toggle hidden-xs"><i class="icon-paragraph-justify3"></i></a>#}
            {#</li>#}
        {#</ul>#}
        <ul class="nav navbar-nav navbar-right">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="dropdown dropdown-user">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {% if app.user.profilePicture %}
                            <img src="{{ app.user.profilepicture }}" alt="">
                        {% else %}
                            <img src="{{ asset('images/default_user.png') }}" alt="">
                        {% endif %}
                        <span>{{ app.user.firstname }}</span>
                        <i class="caret"></i>
                    </a>

                    <ul class="dropdown-menu dropdown-menu-right">
                        <li><a href="{{ path('logout') }}"><i class="icon-switch2"></i> Se déconnecter</a></li>
                    </ul>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('facebook_login') }}" data-popup="tooltip" title="All activity"><i
                                class="icon-lock display-block"></i></a>
                </li>
            {% endif %}
            <li>
                <p class="navbar-text"><span class="label label-flat border-info text-info-600">BETA</span></p>
            </li>
        </ul>
    </div>
</div>