<div class="sidebar sidebar-main sidebar-fixed">
    <div class="sidebar-content">
        <!-- Main navigation -->
        <div class="sidebar-category sidebar-category-visible">
            <div class="category-content no-padding">
                <ul class="navigation navigation-main navigation-accordion">
                    <!-- Main -->
                    <li class="active">
                        <a href="{{ path('bdd_index') }}">
                            <i class="icon-home4"></i> <span>Accueil</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('coaster_ranking') }}">
                            <i class="icon-trophy2"></i> <span>Meilleurs coasters</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('user_list') }}">
                            <i class="icon-users4"></i> <span>Palmarès des membres</span>
                        </a>
                    </li>
                    {% if is_granted('ROLE_PREVIEW_FEATURE') %}
                    <li>
                        <a href="{{ path('map_index') }}">
                            <i class="icon-location3"></i> <span>Carte des coasters</span>
                        </a>
                    </li>
                    {% endif %}
                    <li>
                        <a href="https://coastersworld.fr/"><i class="icon-newspaper2"></i> <span>Actualités</span></a>
                    </li>
                    <li>
                        <a href="https://forum.coastersworld.fr/"><i class="icon-bubble2"></i> <span>Forum</span></a>
                    </li>
                    <li>
                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                            <a href="{{ path('logout') }}"><i class="icon-switch2"></i> <span>Se déconnecter</span></a>
                        {% else %}
                            <a href="{{ path('facebook_login') }}"><i class="icon-lock"></i> <span>Se connecter</span></a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
        <!-- /main navigation -->
    </div>
</div>