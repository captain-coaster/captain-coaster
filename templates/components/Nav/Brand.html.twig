{#
  The full logo, linking Home, at 28px high (193px wide, above DESIGN.md's
  180px minimum).
#}
<a href="{{ path('default_index') }}" {{ attributes.defaults({class: 'inline-flex shrink-0 rounded-control'}) }}>
  <twig:Logo class="h-7 w-auto"/>
</a>
