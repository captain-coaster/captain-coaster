{% extends "base.html.twig" %}

{% import "helper.html.twig" as helper %}

{% set title = coaster.name %}
{% set subTitle = coaster.park.name %}
{% block title %}{{ title }} • {{ subTitle }}{% endblock %}
{% if coaster.formerNames %}
    {% set secondaryTitle = coaster.formerNames|join(', ') %}
{% endif %}

{# {% block header %} #}
    {# <meta name="description" content="{{ coaster.name }} is a {{ coaster.manufacturer.name }} roller coaster"/> #}
{# {% endblock %} #}

{% block body %}
    <div class="grid grid-cols-12 gap-5 items-start">
        <div class="col-span-12 tablet:col-span-3">
            {% if coaster.isRateable %}
                <!-- score -->
                {% if coaster.score %}
                    <div class="cc-panel cc-panel__body">
                        <div class="grid grid-cols-2 gap-5">
                            <div>
                                <div class="text-muted text-size-small text-uppercase score-label">
                                    {{ 'coaster.score.title'|trans }}
                                </div>
                                <div class="score-compact">
                                    <div>
                                        <span class="score-number">{{ coaster.score|format_number({'min_fraction_digit': 1, 'max_fraction_digit': 1}) }}</span><span class="score-percent">%</span>
                                    </div>
                                </div>
                            </div>
                            {% set perPage = constant('App\\Controller\\RankingController::COASTERS_PER_PAGE') %}
                            {% set page = (coaster.rank / perPage)|round(0, 'ceil') %}
                            <div>
                                <a href="{{ path('ranking_index', {'page': page}) }}" class="text-default rank-link">
                                    <div class="text-muted text-size-small text-uppercase score-label">
                                        {{ 'coaster.score.rank'|trans }}
                                    </div>
                                    <div class="rank-number">#{{ coaster.rank }}</div>
                                </a>
                            </div>
                        </div>
                        {% if countRatings %}
                            <div class="rating-distribution-compact">
                                <div class="text-muted text-size-small text-uppercase score-label mb-10">
                                    {{ 'coaster.rating.distribution'|trans }}
                                </div>
                                {% set totalRatings = 0 %}
                                {% for countRating in countRatings %}
                                    {% set totalRatings = totalRatings + countRating.count %}
                                {% endfor %}

                                {% if totalRatings > 0 %}
                                    <div class="rating-bar-container">
                                        <div class="rating-bar">
                                            {% for countRating in countRatings %}
                                                {% set percentage = (countRating.count / totalRatings * 100)|round(2) %}
                                                {% if percentage > 0 %}
                                                    <div class="rating-segment rating-{{ countRating.value|replace({'.': '-'}) }}"
                                                         style="width: {{ percentage }}%"
                                                         title="{{ countRating.value }}★: {{ percentage }}% ({{ countRating.count }} {{ 'coaster.rating.ratings'|trans }})">
                                                        {% if percentage > 7 %}
                                                            <span class="rating-segment-label">{{ countRating.value }}★</span>
                                                        {% endif %}
                                                    </div>
                                                {% endif %}
                                            {% endfor %}
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        {% endif %}
                    </div>
                {% endif %}
                <!-- /score -->
                {% include 'Coaster/_rating_panel.html.twig' with {'coaster': coaster, 'rating': rating, 'user': user} only %}
            {% endif %}
            <!-- main carac -->
            <div class="content-group">
                <div class="grid grid-cols-2 gap-0 cc-button-group--block">
                    <div>
                        <button type="button" class="cc-button cc-button--default cc-button--block cc-button--float cc-button--float-lg">
                            <div class="text-center">
                                {{ ux_icon('heroicons:arrow-trending-up', {'class': 'w-7 h-7 text-warning-600'}) }}
                                <div class="text-bold mt-5">
                                    {% if coaster.height is not empty %}
                                        {{ units.metersOrFeet(coaster.height) }}
                                    {% else %}
                                        &nbsp;
                                    {% endif %}
                                </div>
                            </div>
                        </button>
                        <button type="button" class="cc-button cc-button--default cc-button--block cc-button--float cc-button--float-lg">
                            <div class="text-center">
                                {{ ux_icon('heroicons:rocket-launch', {'class': 'w-7 h-7 text-success-400'}) }}
                                <div class="text-bold mt-5">
                                    {% if coaster.speed is not empty %}
                                        {{ units.kphOrMph(coaster.speed) }}
                                    {% else %}
                                        &nbsp;
                                    {% endif %}
                                </div>
                            </div>
                        </button>
                    </div>
                    <div>
                        <button type="button" class="cc-button cc-button--default cc-button--block cc-button--float cc-button--float-lg">
                            <div class="text-center">
                                {{ ux_icon('heroicons:arrows-right-left', {'class': 'w-7 h-7 text-cc-blue'}) }}
                                <div class="text-bold mt-5">
                                    {% if coaster.length is not empty %}
                                        {{ units.metersOrFeet(coaster.length) }}
                                    {% else %}
                                        &nbsp;
                                    {% endif %}
                                </div>
                            </div>
                        </button>
                        <button type="button" class="cc-button cc-button--default cc-button--block cc-button--float cc-button--float-lg">
                            <div class="text-center">
                                {{ ux_icon('heroicons:arrow-path-rounded-square', {'class': 'w-7 h-7 text-danger-400'}) }}
                                <div class="text-bold mt-5">
                                    {{ coaster.inversionsNumber|default('0') }}
                                </div>
                            </div>
                        </button>
                    </div>
                </div>
            </div>
            <!-- /main carac -->
            <!-- coaster details -->
            <div class="cc-panel cc-cc-panel--white">
                <div class="cc-panel__heading">
                    <h6>
                        {{ 'coaster.infos.title'|trans({'%name%': coaster.name}) }}
                    </h6>
                </div>
                <div class="cc-list-group no-border">
                    {% if coaster.status %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.status'|trans }}
                                :</label>
                            <div class="pull-right">
              <span class="label label-{{ coaster.status.type }} bg-{{ coaster.status.type }}">
                  {{ coaster.status.name|trans([], 'database') }}
              </span>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.park %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.park'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('park_show', {'id': coaster.park.id, 'slug': coaster.park.slug}) }}">{{ coaster.park.name }}</a>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.park.country %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.country'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('ranking_index', {'filters[country]': coaster.park.country.id}) }}">
                                    {{ coaster.park.country.name|trans([], 'database') }}</a>
                            </div>
                        </div>
                    {% endif %}
                    <div class="cc-list-group__item">
                        <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.manufacturer'|trans }}
                            :</label>
                        <div class="pull-right">
                            {% if coaster.manufacturer %}
                                <a href="{{ path('ranking_index', {'filters[manufacturer]': coaster.manufacturer.id}) }}">
                                    {{ coaster.manufacturer.name }}</a>
                            {% else %}
                                <label>{{ 'data.unknown'|trans([], 'database') }}</label>
                            {% endif %}
                        </div>
                    </div>
                    {% if coaster.materialType %}
                        <div class="cc-list-group__item">
                            <label
                                class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.materialType'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('ranking_index', {'filters[materialType]': coaster.materialType.id}) }}">
                                    {{ coaster.materialType.name }}</a>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.seatingType %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.seatingType'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('ranking_index', {'filters[seatingType]': coaster.seatingType.id}) }}">
                                    {{ coaster.seatingType.name }}</a>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.model %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.model'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('ranking_index', {'filters[model]': coaster.model.id}) }}">
                                    {{ coaster.model.name }}</a>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.kiddie %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.kiddie'|trans }}
                                :</label>
                            <div class="pull-right">{{ ux_icon('heroicons:check', {'class': 'w-3 h-3'}) }}</div>
                        </div>
                    {% endif %}
                    {% if coaster.launchs|length > 0 %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.launch'|trans }}
                                :</label>
                            <div class="pull-right">
                                {% for launch in coaster.launchs %}
                                    {{ launch|trans([], 'database') }}
                                    {% if not loop.last %}, {% endif %}
                                {% endfor %}
                            </div>
                        </div>
                    {% endif %}
                    <div class="cc-list-group__item">
                        <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.restraint'|trans }}
                            :</label>
                        <div
                            class="pull-right">{{ coaster.restraint.name|default('data.unknown')|trans([], 'database') }}</div>
                    </div>
                    {% if coaster.openingDate %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.opening'|trans }}
                                :</label>
                            <div class="pull-right">
                                <a href="{{ path('ranking_index', {'filters[openingDate]': coaster.openingDate|date('Y') }) }}">
                                    {{ helper.displayDate(coaster.openingDate) }}</a>
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.closingDate %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.closing'|trans }}
                                :</label>
                            <div class="pull-right">
                                {{ helper.displayDate(coaster.closingDate) }}
                            </div>
                        </div>
                    {% endif %}
                    {% if coaster.indoor %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.indoor'|trans }}
                                :</label>
                            <div class="pull-right">{{ ux_icon('heroicons:check', {'class': 'w-3 h-3'}) }}</div>
                        </div>
                    {% endif %}
                    {% if coaster.vr %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.vr'|trans }}:</label>
                            <div class="pull-right">{{ ux_icon('heroicons:check', {'class': 'w-3 h-3'}) }}</div>
                        </div>
                    {% endif %}
                    {% if coaster.price and coaster.currency %}
                        <div class="cc-list-group__item">
                            <label class="cc-field__label no-margin text-semibold">{{ 'coaster.infos.cost'|trans }}
                                :</label>
                            <div class="pull-right">
                                {{ coaster.price|format_currency(coaster.currency, {fraction_digit: 0}) }}
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
            {% if coaster.youtubeId %}
            <a href="https://www.youtube.com/watch?v={{ coaster.youtubeId }}" target="_blank" class="youtube-thumbnail">
                <img src="https://img.youtube.com/vi/{{ coaster.youtubeId }}/maxresdefault.jpg" alt="Watch {{ coaster.name }} on YouTube" loading="lazy">
            </a>
            {% endif %}
            <!-- /coaster details -->
            <!-- coasters in park -->
            <div class="cc-panel cc-cc-panel--white">
                <div class="cc-panel__heading">
                    <h6>
                        {{ 'coaster.parkcoaster'|trans({'%name%': coaster.park.name}) }}
                        &nbsp;<span class="badge badge-primary">{{ coasters|length }}</span>
                    </h6>
                </div>
                <div class="cc-list-group no-border">
                    {% for otherCoaster in coasters %}
                        <div class="cc-list-group__item">
                            <span class="status-mark border-{{ otherCoaster.status.type }} position-left"></span>
                            <a href="{{ path('show_coaster', {'id': otherCoaster.id, 'slug': otherCoaster.slug}) }}">
                                {{ otherCoaster.name }}
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <!-- /coasters in park -->
            <!-- coaster location -->
            <!-- removed until open street maps -->
            <!-- /coaster location -->
        </div>
        <div class="col-span-12 tablet:col-span-9">
            <!-- AI Summary Section (loaded via AJAX) -->
            <div
                data-controller="coaster-summary"
                data-coaster-summary-slug-value="{{ coaster.slug }}"
                data-coaster-summary-locale-value="{{ app.request.locale }}"
                data-coaster-summary-csrf-token-value="{{ csrf_token('summary_feedback') }}"
            >
                <div id="coaster-ai-summary" data-coaster-summary-target="container">
                    <div class="cc-panel cc-cc-panel--flat mb-15">
                        <div class="cc-panel__heading">
                            <h6 class="cc-panel__title">
                                <span class="text-semibold">
                                    {{ ux_icon('heroicons-solid:star', {'class': 'w-4 h-4 position-left'}) }} Captain's Review
                                </span>
                            </h6>
                        </div>
                        <div class="cc-panel__body text-center">
                            {{ ux_icon('heroicons:arrow-path', {'class': 'w-4 h-4 spinner text-muted'}) }}
                            <span class="text-muted ml-5">{{ 'ai_summary.cc-panel.loading'|trans({}, 'ai_summary') }}</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Photos Section (loaded via AJAX with Stimulus) -->
            <div
                data-controller="coaster-images"
                data-coaster-images-slug-value="{{ coaster.slug }}"
                data-coaster-images-locale-value="{{ app.request.locale }}"
                data-coaster-images-total-images-value="{{ coaster.images|length }}"
            >
                <div id="coaster-photos" data-coaster-images-target="container">
                    <div class="cc-panel cc-cc-panel--flat mb-15">
                        <div class="cc-panel__heading">
                            <h6 class="cc-panel__title">
                                <span class="text-semibold">
                                    {{ ux_icon('heroicons:photo', {'class': 'w-4 h-4 position-left'}) }} {{ 'coaster.photos.title'|trans }}
                                </span>
                            </h6>
                        </div>
                        <div class="cc-panel__body text-center">
                            {{ ux_icon('heroicons:arrow-path', {'class': 'w-4 h-4 spinner text-muted'}) }}
                            <span class="text-muted ml-5">{{ 'coaster.photos.loading'|trans }}</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Reviews Section (loaded via AJAX with Stimulus) -->
            <div
                data-controller="coaster-reviews"
                data-coaster-reviews-slug-value="{{ coaster.slug }}"
                data-coaster-reviews-locale-value="{{ app.request.locale }}"
            >
                <div id="coaster-reviews" data-coaster-reviews-target="container">
                    <div class="cc-panel cc-cc-panel--flat mb-15">
                        <div class="cc-panel__heading">
                            <h6 class="cc-panel__title">
                                <span class="text-semibold">
                                    {{ ux_icon('heroicons:chat-bubble-left-right', {'class': 'w-4 h-4 position-left'}) }} {{ 'review.title'|trans }}
                                </span>
                                <form style="display: none">
                                    <input type="hidden" name="page" value="1">
                                    <div>
                                        <select name="filters[sort]">
                                            <option value=""></option>
                                        </select>
                                    </div>
                                </form>
                            </h6>
                        </div>
                        <div class="cc-panel__body text-center">
                            {{ ux_icon('heroicons:arrow-path', {'class': 'w-4 h-4 spinner text-muted'}) }}
                            <span class="text-muted ml-5">{{ 'review.loading'|trans }}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ reprise_entry_script_tags('coaster') }}
    {% include 'Coaster/rating-json-ld.html.twig' with {'coaster': coaster} only %}
{% endblock %}

{% block stylesheets %}
    {# Load coaster-specific CSS #}
    {{ reprise_entry_link_tags('coaster') }}
{% endblock %}
